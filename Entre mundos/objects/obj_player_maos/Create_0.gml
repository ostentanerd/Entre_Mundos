recarregando = false;

// No Create do obj_player_maos
x_offset = 0; // Se você quiser mover a mão um pouco para o lado (ex: 5)
y_offset = -10; // Se você quiser mover a mão um pouco para cima (ex: -5)