

// --- No Create do obj_computador_save ---
meu_prompt = noone;
salvando_texto = false;
timer_texto = 0;
distancia_ativacao = 40;

// Se o save já foi usado nesta fase, o PC já nasce visualmente desligado
if (global.save_bloqueado) {
    //image_blend = c_dkgray; 
}